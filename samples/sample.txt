PROGRAM p
BEGIN

	INT a,b,c;
    STRING newline := "\n";
    FLOAT x, y, z;
	
	FUNCTION VOID main()
	BEGIN
        a := (1+2)+3;
        a := 20;
        b := (a+b)*2;
        x := 1.0*3.5;

        READ (c, newline, a, b);
        READ(c, a)
        WRITE (a);
        WRITE (a, newline);

	END
END
